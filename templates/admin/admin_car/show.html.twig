{% extends 'admin/base.html.twig' %}

{% block title %}Voiture{% endblock %}

{% block content %}
    <h1>Voitures</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ car.id }}</td>
            </tr>
            <tr>
                <th>NbSeats</th>
                <td>{{ car.nbSeats }}</td>
            </tr>
            <tr>
                <th>NbDoors</th>
                <td>{{ car.nbDoors }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ car.name }}</td>
            </tr>
            <tr>
                <th>Cost</th>
                <td>{{ car.cost }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_car_index') }}">Retour</a>

    <a href="{{ path('app_admin_car_edit', {'id': car.id}) }}">edit</a>

    {{ include('admin/admin_car/_delete_form.html.twig') }}
{% endblock %}
